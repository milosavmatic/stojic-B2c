import React from 'react';
import classes from '../../assets/css/Login.module.scss';

const Login = () => {
  return (
    <div className={`${classes['loginRegister']}`}>
      <div className="container">
        <h1 className={`${classes['name']}`}>ruza</h1>
      </div>
    </div>
  );
};

export default Login;
